<Window x:Class="StreamOrchestra.App.Views.RecordingCredentialsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SOOP 계정 확인"
        Width="460"
        Height="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#0E1724"
        Foreground="#F5F7FB"
        FontFamily="Segoe UI, Malgun Gothic">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#F5F7FB" /></Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="11,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#121F2E" />
            <Setter Property="Foreground" Value="#F5F7FB" />
            <Setter Property="BorderBrush" Value="#324257" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="11,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#121F2E" />
            <Setter Property="Foreground" Value="#F5F7FB" />
            <Setter Property="BorderBrush" Value="#324257" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="17,0" />
            <Setter Property="Background" Value="#18283A" />
            <Setter Property="Foreground" Value="#F5F7FB" />
            <Setter Property="BorderBrush" Value="#34465C" />
        </Style>
    </Window.Resources>
    <Grid Margin="26,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="구독자 전용 방송 계정" FontSize="21" FontWeight="SemiBold" />
        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="SOOP ID" Margin="0,0,0,6" FontWeight="SemiBold" />
            <TextBox x:Name="UsernameTextBox" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock Text="비밀번호" Margin="0,0,0,6" FontWeight="SemiBold" />
            <PasswordBox x:Name="PasswordBox" />
            <TextBlock Text="비밀번호는 저장되지 않습니다."
                       Foreground="#91A0B3"
                       FontSize="12"
                       Margin="0,7,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" IsCancel="True" Margin="0,0,9,0" />
            <Button Content="녹화 시작" Background="#176CE7" BorderBrush="#3185F5" Click="StartButton_Click" />
        </StackPanel>
    </Grid>
</Window>
